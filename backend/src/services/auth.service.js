const register = async (userData) => {
    return userData;
}

const login = async (userData) => {
    return userData;
}

const authService = {
    login,
    register
};

export default authService;